$.ajax({
  url: `/projects/${projectId}/chats/create_private_chat/${useId}`
  // method: 'POST',
  // data: {
  //   user_id: userId,
  //   user_id: userId,
  //   authenticity_token: $('meta[name="csrf-token"]').attr('content') // Ensure this is included
  // },
  // error: function(xhr, status, error) {
  //   alert('Request failed: ' + error);
  // }
});
