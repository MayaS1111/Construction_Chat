<% if project.project_type == "private" && project.owner == current_user || project.project_type == "public" %>
    <%# <% if projects_displayed.include?()%> 
    <li class="p-2 border-bottom" style="border-bottom: 1px solid rgba(255,255,255,.3) !important;">
      <a href= "/chat/<%=project.id%>/<%=project.chats.first.id%>" class="d-flex justify-content-between link-dark text-decoration-none">
        <div class="d-flex fjustify-content-start align-items-center">
          <img src="https://api.dicebear.com/9.x/icons/svg?seed=Casper&backgroundColor=D2042D" alt="avatar"
            class="rounded-circle d-flex align-self-center me-3 shadow-1-strong" width="60"> 
          <div class="pt-1 d-flex align-items-center">
            <p class="fw-bold mb-0"><%=project.name%></p> 
          </div>
          <% if project.project_type == "public"%>
          <div class="col">
            <div class="d-flex d-flex justify-content-center">
              <a type="button" data-bs-toggle="modal" data-bs-target="#<%=project.id%>_info_Modal">
                <i class="fa-solid fa-circle-info"></i>
              </a>
            </div>
            <div class="d-flex">
              <%= render "projects/dropdown_button", project: project%>
            </div>
            </div>
          <% end %>  
        </div>
      </a>
    </li>
    <hr>
<% end %>
