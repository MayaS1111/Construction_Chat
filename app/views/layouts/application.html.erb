<!DOCTYPE html>
<html>
  <head>
    <title>BuiltBetter Chat</title>
    <meta property="og:image" content="meta-image.png">
    <meta name="description" content="Construction Chat. Built to help construction get things Built Better!">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= render "shared/meta_tags" %>
    <%= render "shared/cdn_assets" %>
    <%= render "shared/fonts" %>
    <%= render "shared/styles" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render "shared/navbar" %>
    <div class="container">
      <div class="row mb-2">
          <% if notice.present? %>
            <%= render partial: "shared/flash", locals: { message: notice, css_class: "success" } %>
          <% end %>
          <% if alert.present? %>
            <%= render partial: "shared/flash", locals: { message: alert, css_class: "danger" } %>
          <% end %>
      </div>  
    <%= yield %>
    </div>
    <%= render "shared/footer" %>
  </body>
  
</html>
